import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content:{
        margin:15,
        marginTop:20,
    },

    title:{
        fontSize:20,
        fontWeight:"bold",
        marginBottom:15
    }

});
